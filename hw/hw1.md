# HW1 語言模型初體驗（09/21 – 10/11，寬限至 10/18）

筆記本：[hw1/hw1_llm_basics.ipynb](hw1/hw1_llm_basics.ipynb)　模型：`Qwen/Qwen2.5-1.5B-Instruct`　難度：操作型｜核心段落約 1.5–2 小時，選做另計

## 目標

把第 1–3 單元的概念親手做一遍：token、機率分佈與抽樣、對話模板與 system prompt、提示策略。做完你會知道「模型實際看到什麼、實際算出什麼」。

## 步驟（筆記本各節）

核心段落為第 0–4 節，只有核心段落會出題；第 5、6 節為選做，不計分，有興趣再做。

### 第 0 節 環境與模型
安裝 `transformers`，載入 tokenizer 與模型（固定版本）。第一次載入約 1–2 分鐘。

### 第 1 節 分詞（tokenizer）
- 對 15 個指定字串（中文、英文、數字、符號）計算 token 數並印出切法。
- 觀察：中文常一字一 token 或兩字一 token；英文名字會被切成子詞；**數字逐位切分**；同一句話中英文 token 數相近。
- ✍️ 回答：哪一種文字的 token「最不省」？為什麼這對 API 計費與上下文長度有影響？

### 第 2 節 下一個 token 的機率分佈（fp32）
- 對「臺灣最高的山是哪座？」分別以**原始文字**與**套用對話模板**兩種輸入，印出下一個 token 的前 5 名與機率。
- 觀察：套模板後第一個 token 幾乎確定是「臺灣」（機率很高，請看輸出；模型先重述問題）；不套模板時模型把它當成文章接龍，前幾名是「阿里」「哪」「玉」。
- 用 greedy 解碼把整句生完，看看這個 1.5B 小模型答對了嗎（提示：它常說阿里山）。
- ✍️ 回答：這和第 1 單元「幻覺是文字接龍的自然結果」有什麼關係？

### 第 3 節 抽樣與 temperature
- 同一個提示在 temperature = 0（greedy）、0.7、1.5 各生成 3 次；再固定 temperature = 0.7 比較 top-p = 0.5 與 1.0。
- 觀察：greedy 每次一樣；溫度越高越多樣但越容易離題或出現怪詞。
- ✍️ 回答：如果你要做「客服回覆」與「寫詩」，各會選什麼設定？

### 第 4 節 對話模板與 system prompt
- 印出 `apply_chat_template` 實際產生的字串：看到 `<|im_start|>`、`<|im_end|>` 與預設的 system prompt。
- 計算整段輸入的 token 數。
- 改寫 system prompt（例如「你是逢甲大學的課程助教，只用繁體中文回答」「今天是 2026 年 9 月 21 日」），觀察回答如何改變；試問「今天幾月幾號」在有／無日期資訊時的差別。
- 手動組出三輪對話歷史，觀察模型如何「記得」；把前兩輪刪掉再問「第二高的呢？」看它怎麼答。

### 第 5 節（選做）提示策略小實驗（第 2 單元）
- 內建 12 題小學算術／簡單邏輯題（有標準答案）。
- 比較三種提示：直接問、加「請一步一步思考」（思維鏈）、給 2 個範例（少樣本）。每種跑 2 次，計算正確率。
- ✍️ 回答：哪種最好？兩次結果一樣嗎？這說明評量時為什麼要多跑幾次？

### 第 6 節（選做）模型規模
- 從設定檔讀出層數、隱藏維度、注意力頭數；用程式算出總參數量與 embedding 佔比。
- ✍️ 回答：embedding 為什麼占這麼大比例？和詞彙表大小有什麼關係？

### 最後一格 AI 使用聲明

## 繳交

1. 執行完整份筆記本（保留輸出），下載 `.ipynb`，命名 `hw1_學號.ipynb`，上傳至 iLearn「HW1 作業」。
2. 作答「HW1 實作測驗」（10 題，作答 1 次，答錯可看提示再試）。範圍：第 1、2、4 節的確定性數值（token 數、模板 token 數、前 5 名 token）與第 2、3、4 節的觀察概念題；第 5、6 節不出題。

## 常見問題

- **模型下載很慢／失敗**：Colab 偶爾對 Hugging Face 連線不穩，重跑該格即可；不需要 HF 帳號。
- **記憶體不足**：第 2 節刻意用 fp32 以確保數值穩定，1.5B 在 T4 上約需 7 GB；若失敗請先「執行階段 → 重新啟動」再只跑第 0、2 節。
- **輸出和說明不一樣**：抽樣（第 3 節）本來就會不同；第 1、2、4、6 節在固定版本下應完全一致，若不一致請確認沒有改模型名稱或版本。
